import 'package:flutter/material.dart';

class MediaSosialPage extends StatelessWidget {
  const MediaSosialPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('medsos'),
    );
  }
}
